import { CalculateComissionPipe } from './calculate-comission.pipe';

describe('CalculateComissionPipe', () => {
  it('create an instance', () => {
    const pipe = new CalculateComissionPipe();
    expect(pipe).toBeTruthy();
  });
});
