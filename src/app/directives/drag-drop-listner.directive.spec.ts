import { DragDropListnerDirective } from './drag-drop-listner.directive';

describe('DragDropListnerDirective', () => {
  it('should create an instance', () => {
    const directive = new DragDropListnerDirective();
    expect(directive).toBeTruthy();
  });
});
